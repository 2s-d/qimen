# AI执行指南 - 奇门遁甲小程序开发

## 📋 文档说明

本指南用于指导AI按照`PROJECT_SPEC.md`规范文档完整实现奇门遁甲小程序项目。

---

## 🎯 核心原则

### 1. **严格遵循规范文档**
- 所有实现必须以`PROJECT_SPEC.md`为准
- 不得擅自修改API接口定义
- 不得擅自修改数据库表结构
- 不得擅自修改页面功能需求

### 2. **分阶段执行**
- 按照明确的阶段顺序执行
- 每个阶段完成后进行自检
- 遇到问题记录并继续（不要停顿）

### 3. **遇到问题的处理原则**
- **优先使用推荐依赖库**（见文档1.2节）
- **如果依赖库不可用**：添加TODO注释，使用简化实现继续
- **如果遇到技术难点**：实现简化版本，标注TODO，继续下一步
- **绝不停顿等待**：任何情况下都要继续推进

---

## 🔴 强制性文档引用规则（防止偏离）

**为了防止AI在执行过程中偏离文档或凭记忆编码，必须遵守以下规则：**

### 规则1：每个任务开始前必须查阅文档

**在开始任何实现任务之前，必须执行以下步骤：**

1. **明确当前任务**：我现在要实现什么？（例如：实现用户登录API）

2. **查阅对应文档章节**：
   - 实现API → 查阅 `PROJECT_SPEC.md` 第5章对应的API详细设计
   - 实现页面 → 查阅 `PROJECT_SPEC.md` 第2.2章对应的页面需求
   - 创建数据库表 → 查阅 `PROJECT_SPEC.md` 第4章数据库设计
   - 实现业务逻辑 → 查阅对应的API设计中的"业务逻辑实现"部分

3. **提取关键信息**：
   - 请求参数有哪些？类型是什么？验证规则是什么？
   - 响应格式是什么？成功和失败的响应分别是什么？
   - 需要调用哪些数据库表？字段名是什么？
   - 业务逻辑的步骤是什么？

4. **开始编码**：严格按照提取的信息编写代码

**示例：实现用户登录API**

```
❌ 错误做法（凭记忆）：
"我知道登录API需要username和password，直接写代码..."

✅ 正确做法（查阅文档）：
1. 打开 PROJECT_SPEC.md
2. 定位到 5.1.2 用户登录接口
3. 提取信息：
   - 路径：POST /api/user/login
   - 请求参数：username (string, 必填), password (string, 必填, ≥6位)
   - 成功响应：{ code: 200, message: "登录成功", data: { token, user: { id, username, nickname, qipan_count } } }
   - 失败响应：{ code: 401, message: "账号或密码错误" }
   - 业务逻辑：查询用户 → 验证密码(bcrypt) → 生成JWT(24小时) → 统计起盘次数 → 返回
4. 按照提取的信息编写代码
```

### 规则2：每完成一个子任务后进行文档对照

**完成代码后，必须进行文档对照检查：**

**对照清单：**
- [ ] API路径是否与文档一致？
- [ ] 请求参数名称、类型、验证规则是否与文档一致？
- [ ] 响应格式（成功/失败）是否与文档一致？
- [ ] 业务逻辑步骤是否完整实现？
- [ ] 数据库字段名是否与文档一致？
- [ ] 错误码是否与文档一致？

**如果发现不一致：**
- 立即修正代码，使其符合文档
- 不要修改文档来适应代码

### 规则3：阶段性文档回顾

**每完成一个阶段后，必须进行全面文档回顾：**

**回顾步骤：**
1. 重新打开 `PROJECT_SPEC.md`
2. 检查当前阶段涉及的所有章节
3. 逐项对照已实现的功能
4. 记录任何偏离或遗漏

**回顾报告格式：**
```
📋 阶段X文档对照报告

已实现功能：
- [✓] 功能A - 符合文档X.Y节
- [✓] 功能B - 符合文档X.Z节
- [⚠️] 功能C - 部分偏离文档，已修正

偏离记录：
- 原计划：[文档中的定义]
- 实际实现：[当前代码的实现]
- 修正措施：[如何修正]

遗漏功能：
- [待补充] 功能D - 文档X.W节要求但未实现
```

### 规则4：使用文档章节引用注释

**在代码中添加文档引用注释，保持可追溯性：**

**Python示例：**
```python
# 文档引用：PROJECT_SPEC.md - 5.1.2 用户登录接口
@router.post("/api/user/login")
async def login(request: UserLoginRequest):
    """
    用户登录
    
    文档定义：
    - 请求参数：username (string), password (string, ≥6位)
    - 成功响应：{ code: 200, data: { token, user } }
    - 失败响应：{ code: 401, message: "账号或密码错误" }
    
    业务逻辑（文档5.1.2节）：
    1. 查询用户
    2. 验证密码(bcrypt)
    3. 生成JWT(24小时)
    4. 统计起盘次数
    5. 返回结果
    """
    # 步骤1：查询用户（文档5.1.2 - 步骤1）
    user = await get_user_by_username(request.username)
    if not user:
        raise AuthException("账号或密码错误", code=401)
    
    # 步骤2：验证密码（文档5.1.2 - 步骤2）
    if not verify_password(request.password, user.password_hash):
        raise AuthException("账号或密码错误", code=401)
    
    # ... 后续步骤
```

**JavaScript/Vue示例：**
```javascript
// 文档引用：PROJECT_SPEC.md - 2.2.2 页面2：登录页
// 调用接口：POST /api/user/login（文档5.1.2节）

export default {
  name: 'LoginPage',
  methods: {
    async handleLogin() {
      // 前端校验（文档2.2.2 - 业务逻辑）
      // 1. 账号不能为空
      if (!this.username) {
        uni.showToast({ title: '请输入账号', icon: 'none' })
        return
      }
      
      // 2. 密码长度≥6位
      if (this.password.length < 6) {
        uni.showToast({ title: '密码长度不能少于6位', icon: 'none' })
        return
      }
      
      // 调用登录API（文档5.1.2节）
      const res = await userApi.login({
        username: this.username,
        password: this.password
      })
      
      // 登录成功处理（文档2.2.2 - 交互设计）
      // 1. 存储token到storage
      uni.setStorageSync('token', res.data.token)
      // 2. 存储用户信息到Pinia
      this.userStore.setUserInfo(res.data.user)
      // 3. 跳转回上一页或首页
      uni.navigateBack()
    }
  }
}
```

### 规则5：文档章节速查表

**为了快速定位文档章节，使用以下速查表：**

| 需要实现的内容 | 查阅文档章节 | 关键信息 |
|---------------|-------------|---------|
| 用户注册API | 5.1.1节 | 请求参数、响应格式、业务逻辑5步骤 |
| 用户登录API | 5.1.2节 | 请求参数、响应格式、业务逻辑5步骤 |
| 获取用户信息API | 5.1.3节 | 需要JWT认证、响应格式、业务逻辑3步骤 |
| 修改用户信息API | 5.1.4节 | 需要JWT认证、仅支持修改昵称 |
| 起盘计算API | 5.2.1节 | 请求参数、响应格式、业务逻辑4步骤 |
| 保存起盘记录API | 5.2.2节 | 需要JWT认证、100条限制 |
| 获取历史列表API | 5.2.3节 | 需要JWT认证、分页参数、业务逻辑 |
| 登录页 | 2.2.2节 | 页面布局、交互设计、业务逻辑 |
| 注册页 | 2.2.3节 | 页面布局、交互设计、业务逻辑 |
| 起盘输入页 | 2.2.4节 | 页面布局、交互设计、业务逻辑 |
| 盘面展示页 | 2.2.5节 | 页面布局、交互设计、业务逻辑 |
| users表 | 4.2.1节 | 字段定义、索引、业务规则 |
| qimen_records表 | 4.2.2节 | 字段定义、索引、业务规则、100条限制 |
| 统一响应格式 | 6.1节 | 成功/失败响应格式、状态码 |
| JWT认证 | 6.2节 | Token格式、有效期24小时 |

### 规则6：偏离文档的处理流程

**如果发现自己的实现偏离了文档，必须立即执行以下流程：**

1. **停止当前编码**
2. **重新查阅文档**：找到对应章节，仔细阅读
3. **对比差异**：列出文档要求 vs 当前实现的差异
4. **决策**：
   - 如果文档是对的 → 修改代码以符合文档
   - 如果文档有明显错误 → 添加TODO注释说明，继续按文档执行
5. **记录偏离**：在进度报告中记录这次偏离和修正

**偏离记录模板：**
```
⚠️ 偏离记录 #X

发现时间：阶段X，任务Y
偏离内容：[具体描述]
文档要求：[文档X.Y节的定义]
实际实现：[当前代码的实现]
差异原因：[为什么会偏离]
修正措施：[如何修正]
修正状态：[✓已修正 / ⏳待修正 / ❌无法修正]
```

---

## 📌 文档引用检查点

**在以下关键节点，必须强制查阅文档：**

✅ **每个阶段开始时**：重新阅读该阶段涉及的所有文档章节

✅ **每个API实现前**：查阅对应的API详细设计（第5章）

✅ **每个页面实现前**：查阅对应的页面需求（第2.2章）

✅ **每个数据库表创建前**：查阅对应的表结构定义（第4章）

✅ **每个阶段完成后**：进行文档对照检查

✅ **发现问题时**：立即查阅文档确认正确做法

✅ **每天开始工作前**（如果跨天）：重新阅读PROJECT_SPEC.md的核心章节

---

## 💡 防止文档偏离的最佳实践

1. **打开两个窗口**：一个窗口编码，一个窗口显示PROJECT_SPEC.md
2. **逐节实现**：不要跳跃式开发，按文档顺序逐节实现
3. **频繁对照**：每写10-20行代码就对照一次文档
4. **使用文档原文**：直接复制文档中的字段名、参数名，不要凭记忆
5. **注释引用章节**：在代码中标注对应的文档章节号
6. **定期回顾**：每小时回顾一次PROJECT_SPEC.md的目录，确保方向正确

**记住：文档是唯一的真理来源，任何偏离都必须修正！**

---

## 📝 执行检查清单

### 阶段0：环境准备（预计10分钟）

**任务清单：**
- [ ] 创建项目根目录 `qimen-miniapp/`
- [ ] 创建后端目录 `backend/`
- [ ] 创建前端目录 `frontend/`
- [ ] 初始化后端项目（FastAPI）
- [ ] 初始化前端项目（uni-app）
- [ ] 安装推荐依赖库（见文档1.2节）

**自检问题：**
1. 项目目录结构是否符合文档3.1节的定义？
2. 所有推荐依赖库是否已安装？
3. 如果某个库安装失败，是否已添加TODO注释？

**完成标志：**
- 后端可以运行 `uvicorn main:app --reload`
- 前端可以运行 `npm run dev:h5`（H5模式，浏览器预览）

**🔴 关键说明：开发和预览方式**

**前端开发模式选择：**

本项目使用uni-app开发，支持多种编译模式。**强烈建议使用H5模式进行开发和测试**：

1. **H5模式（推荐）**：
   - 命令：`npm run dev:h5`
   - 预览：在浏览器中打开 `http://localhost:8080`
   - 优点：无需任何额外工具，开发调试最方便
   - 适用场景：开发阶段的所有功能测试

2. **微信小程序模式（可选，仅最终测试）**：
   - 命令：`npm run dev:mp-weixin`
   - 预览：需要微信开发者工具
   - 使用时机：仅在最终部署前测试小程序特有功能

**如果没有微信开发者工具怎么办？**

**完全不影响开发！** 按照以下策略执行：

1. **开发阶段**：
   - 使用H5模式：`npm run dev:h5`
   - 在Chrome/Firefox等浏览器中预览和测试
   - 所有功能（登录、起盘、历史记录等）都可以在浏览器中完整测试

2. **如果遇到微信开发者工具相关问题**：
   - 添加TODO注释：
     ```javascript
     // TODO: 微信开发者工具不可用，使用H5模式开发
     // 原因：开发环境无法安装微信开发者工具
     // 影响：无法测试小程序特有API（如wx.login），但不影响核心业务功能
     // 计划：部署前在有微信开发者工具的环境中测试
     // 优先级：P2（非阻塞）
     ```
   - 继续使用H5模式开发

3. **package.json配置**：
   ```json
   {
     "scripts": {
       "dev": "npm run dev:h5",
       "dev:h5": "uni -p h5",
       "dev:mp-weixin": "uni -p mp-weixin",
       "build:h5": "uni build -p h5",
       "build:mp-weixin": "uni build -p mp-weixin"
     }
   }
   ```

**前端初始化命令（推荐）**：

```bash
# 方式1：使用Vue CLI创建uni-app项目（推荐）
npm install -g @vue/cli
vue create -p dcloudio/uni-preset-vue frontend

# 方式2：使用HBuilderX CLI（如果可用）
# 如果HBuilderX不可用，使用方式1

# 进入项目目录
cd frontend

# 安装依赖
npm install

# 启动H5开发服务器
npm run dev:h5
```

**验证前端启动成功**：
- 命令行显示：`App running at: http://localhost:8080`
- 浏览器访问：`http://localhost:8080`
- 看到uni-app默认首页

**后端初始化命令**：

```bash
# 创建后端目录
mkdir backend
cd backend

# 创建虚拟环境（推荐）
python -m venv venv

# 激活虚拟环境
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# 创建requirements.txt
cat > requirements.txt << EOF
fastapi==0.104.1
uvicorn[standard]==0.24.0
sqlalchemy==2.0.23
pydantic==2.5.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6
kinqimen==0.1.8
sxtwl==1.1.0
alembic==1.12.1
EOF

# 安装依赖
pip install -r requirements.txt

# 创建main.py
cat > main.py << EOF
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware

app = FastAPI(title="奇门遁甲API")

# CORS配置
app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:8080"],  # H5开发服务器地址
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

@app.get("/")
def read_root():
    return {"message": "奇门遁甲API服务运行中"}

@app.get("/api/health")
def health_check():
    return {"status": "ok"}
EOF

# 启动后端服务
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

**验证后端启动成功**：
- 命令行显示：`Uvicorn running on http://0.0.0.0:8000`
- 浏览器访问：`http://localhost:8000` 看到 `{"message": "奇门遁甲API服务运行中"}`
- 访问API文档：`http://localhost:8000/docs` 看到Swagger UI

**环境准备完成的标志**：
- ✅ 后端服务运行在 `http://localhost:8000`
- ✅ 前端H5服务运行在 `http://localhost:8080`
- ✅ 浏览器可以访问前端页面
- ✅ 浏览器可以访问后端API文档

**如果遇到问题**：
- Python依赖安装失败 → 添加TODO，使用简化实现继续
- Node.js依赖安装失败 → 检查网络，尝试使用国内镜像：`npm config set registry https://registry.npmmirror.com`
- 端口被占用 → 更换端口（前端8081，后端8001）

**记住：H5模式可以完成所有开发和测试，微信开发者工具不是必需的！**

---

### 阶段1：数据库设计（预计30分钟）

**📖 开始前必读：PROJECT_SPEC.md 第4章 - 数据库设计**

**任务清单：**
- [ ] **查阅文档**：仔细阅读PROJECT_SPEC.md第4章，理解6张表的完整定义
- [ ] 创建6张数据库表（严格按照文档4节定义）
  - [ ] users表（文档4.2.1节）- 检查字段名、类型、约束、索引
  - [ ] qimen_records表（文档4.2.2节）- 检查字段名、类型、约束、索引、100条限制
  - [ ] knowledge_articles表（文档4.2.3节）- 检查字段名、类型、约束、索引
  - [ ] system_config表（文档4.2.4节）- 检查字段名、类型、约束、索引
  - [ ] announcements表（文档4.2.5节）- 检查字段名、类型、约束、索引
  - [ ] feedback表（文档4.2.6节）- 检查字段名、类型、约束、索引
- [ ] 编写数据库初始化脚本
- [ ] 准备初始数据（系统配置、知识文章）
- [ ] **文档对照检查**：逐表对照文档，确认字段名、类型、约束完全一致

**自检问题：**
1. 每张表的字段是否与文档4节完全一致？（字段名、类型、长度、约束）
2. 主键、外键、索引是否都已创建？
3. 初始数据是否已准备好？
4. 是否有任何字段名或类型与文档不一致？（如有，立即修正）

**完成标志：**
- 数据库初始化脚本可以成功运行
- 6张表都已创建
- 初始数据已插入
- 所有表结构与文档4章完全一致（逐字段对照）

**遇到问题怎么办：**
- 如果SQLAlchemy有问题：使用原生SQL继续
- 如果初始数据准备困难：先用最少数据（1-2条），标注TODO
- 如果字段定义不清楚：重新查阅文档4章对应小节，不要猜测

---

### 阶段2：后端基础架构（预计30分钟）

**任务清单：**
- [ ] 实现统一响应格式（文档6.1节）
- [ ] 实现JWT认证中间件（文档6.2节）
- [ ] 实现全局异常处理（文档6.3节）
- [ ] 实现请求拦截器
- [ ] 实现日志记录

**自检问题：**
1. 响应格式是否符合文档6.1节定义？
2. JWT Token格式是否符合文档6.2.1节？
3. 异常处理是否包含所有自定义异常类？

**完成标志：**
- 可以成功生成和验证JWT Token
- 统一响应格式正常工作
- 异常处理正常工作

**遇到问题怎么办：**
- 如果python-jose有问题：使用PyJWT替代
- 如果passlib有问题：使用hashlib+salt简化实现

---

### 阶段3：用户模块（预计1小时）

**📖 开始前必读：**
- **PROJECT_SPEC.md 第5.1章** - 用户模块接口（4个API）
- **PROJECT_SPEC.md 第2.2章** - 页面2、3、9的详细需求

**任务清单：**
- [ ] **查阅文档**：仔细阅读5.1章（用户模块4个API）和2.2章（页面2、3、9）
- [ ] 实现4个用户API（严格按照文档5.1节）
  - [ ] **查阅5.1.1节** → POST /api/user/register（注册）
    - 检查：请求参数（username、password、nickname）
    - 检查：响应格式（成功200、失败400）
    - 检查：业务逻辑5步骤（参数验证、唯一性检查、密码加密、创建记录、返回结果）
  - [ ] **查阅5.1.2节** → POST /api/user/login（登录）
    - 检查：请求参数（username、password）
    - 检查：响应格式（成功200返回token和user、失败401）
    - 检查：业务逻辑5步骤（查询用户、验证密码、生成JWT、统计起盘次数、返回结果）
  - [ ] **查阅5.1.3节** → GET /api/user/info（获取用户信息）
    - 检查：需要JWT认证
    - 检查：响应格式（包含id、username、nickname、qipan_count、created_at）
    - 检查：业务逻辑3步骤（获取用户、统计起盘次数、返回结果）
  - [ ] **查阅5.1.4节** → PUT /api/user/info（修改用户信息）
    - 检查：需要JWT认证
    - 检查：仅支持修改昵称
    - 检查：响应格式和业务逻辑4步骤
- [ ] 实现3个用户页面（严格按照文档2.2节）
  - [ ] **查阅2.2.2节** → 登录页（页面2）
    - 检查：页面布局（Logo、账号输入框、密码输入框、登录按钮）
    - 检查：交互设计（前端校验、调用登录API、存储token、跳转）
    - 检查：业务逻辑（账号非空、密码≥6位）
    - 参考：template/2-login.html
  - [ ] **查阅2.2.3节** → 注册页（页面3）
    - 检查：页面布局（用户名、密码、确认密码、昵称输入框）
    - 检查：交互设计（前端校验、调用注册API、跳转登录页）
    - 检查：业务逻辑（用户名3-20位、密码6-20位、确认密码一致）
    - 参考：template/3-register.html
  - [ ] **查阅2.2.9节** → 个人中心页（页面9）
    - 检查：页面布局（用户信息卡片、功能列表、退出登录按钮）
    - 检查：交互设计（修改资料、意见反馈、关于我们、退出登录）
    - 检查：业务逻辑（未登录显示"请先登录"、退出需二次确认）
    - 参考：template/9-profile.html
- [ ] **文档对照检查**：逐个API和页面对照文档，确认完全一致

**自检问题：**
1. API请求/响应格式是否与文档5.1章完全一致？（字段名、类型、错误码）
2. 页面布局是否参考了template/对应的html文件？
3. 密码是否已加密存储（bcrypt）？
4. Token是否正确生成和存储（JWT、24小时有效期）？
5. 前端校验规则是否与文档一致？（用户名3-20位、密码≥6位等）

**完成标志：**
- 可以成功注册新用户（密码bcrypt加密）
- 可以成功登录并获取Token（JWT、24小时有效期）
- 可以查看和修改用户信息（仅昵称）
- 前端页面可以正常显示和交互
- 所有API响应格式与文档5.1章完全一致

**遇到问题怎么办：**
- 如果前端UI复杂：先实现功能，UI简化，标注TODO
- 如果某个API有问题：先返回模拟数据，标注TODO
- 如果字段名不确定：重新查阅文档5.1章对应小节，不要猜测

---

### 阶段4：奇门遁甲核心（预计1.5小时）

**任务清单：**
- [ ] 集成kinqimen库实现起盘算法
- [ ] 实现8个算命API（严格按照文档5.2节）
  - [ ] POST /api/qimen/calculate（核心）
  - [ ] POST /api/qimen/save
  - [ ] GET /api/qimen/history
  - [ ] GET /api/qimen/history/{id}
  - [ ] DELETE /api/qimen/history/{id}
  - [ ] GET /api/qimen/fortune
  - [ ] GET /api/tools/calendar
  - [ ] GET /api/knowledge/articles
- [ ] 实现7个算命页面（严格按照文档2.2节）
  - [ ] 起盘输入页（页面4）
  - [ ] 盘面展示页（页面5）
  - [ ] 解读结果页（页面6）
  - [ ] 历史列表页（页面7）
  - [ ] 历史详情页（页面8）
  - [ ] 万年历页（页面10）
  - [ ] 基础知识页（页面11）

**自检问题：**
1. kinqimen库是否成功调用？
2. 起盘结果是否正确转换为API定义的格式？
3. 九宫格盘面是否正确显示？
4. 历史记录是否限制100条？

**完成标志：**
- 可以成功起盘并显示九宫格
- 可以保存和查看历史记录
- 万年历可以正常显示
- 知识文章可以正常显示

**遇到问题怎么办：**
- 如果kinqimen不可用：使用随机生成的盘面数据，标注TODO
- 如果sxtwl不可用：使用简化的日期转换，标注TODO
- 如果九宫格UI复杂：先用表格显示，标注TODO

---

### 阶段5：系统模块（预计45分钟）

**任务清单：**
- [ ] 实现6个系统API（严格按照文档5.3节）
  - [ ] GET /api/system/config
  - [ ] GET /api/system/help
  - [ ] POST /api/system/feedback
  - [ ] GET /api/system/announcements
  - [ ] GET /api/system/statistics
  - [ ] GET /api/system/health
- [ ] 实现2个系统页面（严格按照文档2.2节）
  - [ ] 首页（页面1）
  - [ ] 关于/帮助页（页面12）

**自检问题：**
1. 首页是否包含轮播图、今日运势、功能宫格、公告？
2. 系统配置是否有缓存机制？
3. 健康检查接口是否正常？

**完成标志：**
- 首页可以正常显示所有元素
- 系统配置可以正常获取
- 关于页面可以正常显示

**遇到问题怎么办：**
- 如果轮播图复杂：先用静态图片，标注TODO
- 如果缓存实现困难：先不缓存，标注TODO

---

### 阶段6：联调测试（预计1小时）

**任务清单：**
- [ ] 测试所有18个API接口
- [ ] 测试所有12个页面
- [ ] 测试用户注册登录流程
- [ ] 测试起盘保存查看流程
- [ ] 测试历史记录增删查
- [ ] 修复发现的Bug

**自检问题：**
1. 所有API是否返回正确的响应格式？
2. 所有页面是否可以正常跳转？
3. Token认证是否正常工作？
4. 是否有明显的Bug？

**完成标志：**
- 可以完整走通用户注册→登录→起盘→查看历史的流程
- 所有API都能正常响应
- 所有页面都能正常显示

**遇到问题怎么办：**
- 如果发现Bug：优先修复阻塞性Bug
- 如果非阻塞性Bug：记录到TODO列表

---

## 🚨 关键注意事项

### 1. **绝不停顿的原则**

遇到以下情况，按照对应策略继续：

| 问题类型 | 处理策略 | 示例 |
|---------|---------|------|
| 依赖库不可用 | 添加TODO，使用简化实现 | kinqimen不可用→随机生成盘面 |
| API实现困难 | 先返回模拟数据，标注TODO | 复杂计算→返回固定值 |
| UI实现复杂 | 简化UI，保证功能，标注TODO | 九宫格→表格显示 |
| 数据准备困难 | 使用最少数据，标注TODO | 知识文章→只准备1-2条 |
| 测试失败 | 记录问题，继续下一项 | 某个API失败→记录，测试下一个 |

### 2. **TODO注释规范**

```python
# TODO: [问题描述]
# 原因：[为什么没有完整实现]
# 影响：[对功能的影响程度]
# 计划：[后续如何处理]
# 优先级：[P0/P1/P2]
```

示例：
```python
# TODO: kinqimen库不可用，暂时使用随机生成盘面
# 原因：pip install kinqimen失败，可能是网络问题
# 影响：起盘结果不准确，但不影响整体流程测试
# 计划：解决网络问题后重新安装kinqimen库
# 优先级：P0（核心功能）
```

### 3. **进度报告格式**

每完成一个阶段，输出进度报告：

```
✅ 阶段X完成：[阶段名称]
完成时间：[实际用时]
完成内容：
- [具体完成项1]
- [具体完成项2]

⚠️ 遗留问题：
- [TODO项1]
- [TODO项2]

➡️ 下一步：开始阶段X+1
```

---

## 📊 最终交付检查

完成所有阶段后，进行最终检查：

### 功能完整性检查
- [ ] 18个API接口全部实现
- [ ] 12个页面全部实现
- [ ] 6张数据库表全部创建
- [ ] 用户注册登录流程正常
- [ ] 起盘保存查看流程正常

### 代码质量检查
- [ ] 所有API响应格式符合规范
- [ ] 所有错误处理正常工作
- [ ] JWT认证正常工作
- [ ] 密码已加密存储
- [ ] 日志记录正常

### 文档完整性检查
- [ ] 所有TODO都已记录
- [ ] README.md已创建（包含启动说明）
- [ ] requirements.txt已创建
- [ ] package.json已创建

---

## 🎯 成功标准

项目被认为成功完成，当且仅当：

1. ✅ 可以成功启动后端服务
2. ✅ 可以成功启动前端服务
3. ✅ 可以完整走通核心流程：注册→登录→起盘→查看历史
4. ✅ 所有18个API都能正常响应（即使部分返回模拟数据）
5. ✅ 所有12个页面都能正常显示（即使UI简化）
6. ✅ 所有TODO都已记录在代码中

**即使有TODO项，只要满足以上6条，项目就算成功完成！**

---

## 💡 给AI的最后提示

1. **速度优先于完美**：先让整个系统跑起来，再优化细节
2. **功能优先于UI**：先保证功能正常，UI可以简化
3. **真实数据优先于完美数据**：数据可以少，但要真实可用
4. **持续推进**：任何情况下都不要停顿，记录问题继续前进
5. **相信文档**：所有决策以PROJECT_SPEC.md为准

**记住：你的目标是在6-8小时内交付一个可运行的MVP，而不是一个完美的产品！**

---

## 📞 执行开始

当你准备好开始执行时，请按照以下格式开始：

```
🚀 开始执行奇门遁甲小程序开发
执行时间：[当前时间]
预计完成时间：[当前时间+6小时]

📋 执行计划：
- 阶段0：环境准备（10分钟）
- 阶段1：数据库设计（30分钟）
- 阶段2：后端基础架构（30分钟）
- 阶段3：用户模块（1小时）
- 阶段4：奇门遁甲核心（1.5小时）
- 阶段5：系统模块（45分钟）
- 阶段6：联调测试（1小时）

开始执行阶段0...
```

**现在开始执行！** 🚀
